# Fly.io configuration for Werewolf Arena Frontend

app = "werewolf-arena-frontend"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[http_service.checks]]
  interval = "15s"
  timeout = "10s"
  grace_period = "5s"
  method = "GET"
  path = "/"

[http_service.checks.match]
  status = [200]

[http_service.concurrency]
  type = "requests"
  soft_limit = 200
  hard_limit = 250

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

# Environment variables
[env]
  NODE_ENV = "production"
  NEXT_TELEMETRY_DISABLED = "1"
  BACKEND_URL = "https://werewolf-arena-backend.fly.dev"
  NEXT_PUBLIC_API_URL = "https://werewolf-arena-backend.fly.dev"
  NEXT_PUBLIC_WS_URL = "wss://werewolf-arena-backend.fly.dev"

# Mount for any persistent data (if needed)
#[mounts]
#  source = "data"
#  destination = "/data"

# Dockerfile
[deploy]
  strategy = "canary"
